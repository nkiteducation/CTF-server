[project]
name = "ctf-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128.0",
    "granian>=2.6.1",
    "orjson>=3.11.5",
    "pyzipper>=0.2.6",
]
